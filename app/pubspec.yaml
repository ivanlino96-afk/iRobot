name: airobot
description: Remote control client for AiRobot.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  go_router: ^17.0.0
  mqtt_client: ^10.11.11
  dio: ^5.9.0
  flutter_secure_storage: ^10.0.0
  mobile_scanner: ^7.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
